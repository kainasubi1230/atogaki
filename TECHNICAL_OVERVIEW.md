# あとがき Technical Overview

`あとがき` は、入力テキストを「既存フォントの置き換え」ではなく、文字軌跡データセットから再構成した手書き風 SVG に変換するプロダクトです。  
アクセシビリティ用途に限定しながら、読みやすさ・筆跡らしさ・編集可能性を両立することを狙っています。

## 技術的な見どころ

- **データセット駆動の手書き生成**
  - ひらがな、カタカナ、漢字、英数字、記号を JSONL の軌跡データとして保持。
  - 入力文字に対して候補軌跡を選び、文字ごとの形状を SVG path として再構成します。
  - フォントを傾けるだけではなく、ストローク単位の形・太さ・揺れを扱います。

- **常用漢字 + 実用記号への拡張**
  - KanjiVG 由来の漢字軌跡、かな/カナ用の手書き風データ、句読点・括弧・演算記号などを統合。
  - `。` `、` `「」` `!?` など、文章で使う記号も専用データセット化しています。

- **品質ゲート付きランダム生成**
  - 毎回同じ字にならないよう候補選択・回転・せん断・線幅・末端の払いをランダム化。
  - 一方で、ごちゃごちゃ線や横線だけになる失敗を防ぐため、形状スコア・ストローク数・縦横比・IoU による品質チェックを入れています。

- **ユーザー筆跡プロファイル**
  - ユーザーがアップロードした手書きサンプルから、線幅、傾き、縦横比、揺れを推定。
  - 学習済み文字はユーザー exemplar を優先し、未学習文字はベースデータセットへフォールバックします。

- **編集可能な SVG 出力**
  - 生成結果はラスター画像ではなく SVG path。
  - web 上で文字単位・ストローク単位に選択、移動、削除できる構造にしています。

- **安全性と用途制限**
  - `purpose=accessibility` の生成のみ許可。
  - 出力 SVG には可視ウォーターマークを埋め込み。
  - 同意済みデータのみ学習に使用。
  - API リクエストと生成イベントを監査ログとして保存。

## アーキテクチャ

```text
Next.js / React
  - キャンバス UI
  - 文字入力、変換、SVG 編集
  - Vercel デプロイ想定

FastAPI
  - 認証
  - 生成 API
  - データセット読み込み
  - SVG/軌跡出力

Trainer / Worker
  - 画像前処理
  - ユーザー筆跡プロファイル作成
  - ベースモデル学習

Storage
  - base JSONL datasets
  - base_model.pt
  - adapters / outputs
```

## 生成パイプライン

```text
入力テキスト
  -> 文字種判定
  -> ランタイム文字候補バンク読み込み
  -> 候補軌跡選択
  -> 品質スコアリング
  -> 筆跡ゆらぎ付与
  -> サイズ/字間調整
  -> SVG path 出力
  -> web キャンバス上で編集
```

## なぜ普通のフォント変換ではないか

一般的な「手書き風フォント」は、文字の形が固定されます。  
このプロダクトでは、各文字をストロークデータとして扱うため、以下が可能です。

- 変換ごとに少し違う字を出せる
- はね・払い・線幅を調整できる
- 小さい仮名や句読点の位置を制御できる
- ユーザーごとの筆跡特徴を反映できる
- 生成後にストローク単位で編集できる

## 現在の課題

- 文字単体生成では、文全体の筆跡リズムが弱い。
- 今後は `文字列 -> 連続軌跡` の行単位生成へ移行すると、字間・サイズ・流れがさらに自然になります。
- Vercel には web のみを置き、データセットと生成 API は別サーバーで運用する構成が現実的です。

